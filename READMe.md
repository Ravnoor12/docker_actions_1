## Doing this project to work with docker image deployment and github actions

* A simple web application built using Python and Flask.
* Repo includes automated testing using `pytest` to verify the web pages and user forms work correctly.
* Uses a GitHub Actions CI/CD workflow to automatically run the tests and build a Docker container whenever new code is pushed.